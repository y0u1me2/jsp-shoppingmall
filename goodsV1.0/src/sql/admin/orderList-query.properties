selectOrderList=SELECT * FROM (SELECT ROWNUM AS RNUM,A.* FROM(SELECT * FROM PAYMENTDETAIL PD JOIN CUSTOM C USING(C_NO) JOIN PRODUCT P ON C.P_NO=P.P_NO JOIN PAYMENT PM USING(O_NO) ORDER BY O_DATE DESC) A) WHERE RNUM BETWEEN ? AND ?
totalPrice=SELECT SUM(O_TOTALPRICE) AS SUM FROM PAYMENTDETAIL JOIN PAYMENT USING(O_NO) JOIN CUSTOM USING(C_NO) JOIN PRODUCT USING(P_NO)